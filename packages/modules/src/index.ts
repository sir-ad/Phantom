// PHANTOM Modules package
// This package will host built-in and community-facing module implementations.
// For now, built-in module manifests live in @phantom/core.

export const MODULES_PACKAGE_VERSION = '1.0.0';

export function describeModulesPackage(): string {
  return 'Phantom modules package scaffold. Built-in manifests are currently provided by @phantom/core.';
}
