{
  "name": "@phantom-pm/browser-agent",
  "version": "3.0.3",
  "description": "Autonomous UI manipulation and crawling agent (OpenClaws layer)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "test": "vitest run",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@phantom-pm/core": "3.0.3",
    "playwright": "^1.42.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
