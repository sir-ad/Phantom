{
  "name": "phantom",
  "version": "3.0.0",
  "description": "The invisible force behind every great product. Open source PM operating system for the terminal age.",
  "repository": {
    "type": "git",
    "url": "https://github.com/sir-ad/Phantom.git"
  },
  "private": true,
  "bin": {
    "phantom": "dist/phantom.js"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@phantom-pm/memory && npm run build --workspace=@phantom-pm/core && npm run build --workspace=@phantom-pm/browser-agent && npm run build --workspace=@phantom-pm/docs-engine && npm run build --workspace=@phantom-pm/mcp-server && npm run build --workspace=@phantom-pm/interview-analyzer && npm run build --workspace=@phantom-pm/feedback-hub && npm run build --workspace=@phantom-pm/usage-intelligence && npm run build --workspace=@phantom-pm/discovery-loop && npm run build --workspace=@phantom-pm/agent-communicator && npm run build --workspace=@phantom-pm/modules && npm run build --workspace=@phantom-pm/os-agent && npm run build --workspace=@phantom-pm/tui && npm run build --workspace=@phantom-pm/integrations && npm run build --workspace=@phantom-pm/cli",
    "build:github-dist": "node scripts/release/build-github-distribution.mjs",
    "dev": "npm run dev --workspace=packages/cli",
    "website:dev": "node scripts/serve-website.mjs",
    "phantom": "node packages/cli/dist/index.js",
    "typecheck": "npm run build",
    "deps:check": "node scripts/check-dependency-consistency.mjs",
    "lint": "eslint packages --ext .ts,.tsx",
    "lint:fix": "eslint packages --ext .ts,.tsx --fix",
    "format": "prettier --write \"packages/**/*.{ts,tsx,json,md}\"",
    "format:check": "prettier --check \"packages/**/*.{ts,tsx,json,md}\"",
    "test": "npm run build && node --test tests/smoke.test.mjs tests/mcp-contract.test.mjs",
    "reality:check": "npm run build && node --test tests/reality-gate.test.mjs",
    "release:local": "npm run build && node scripts/release/build-local-install-artifacts.mjs",
    "installer:test-local": "npm run build && node scripts/release/test-local-installer.mjs",
    "agents:upgrade": "sh scripts/upgrade-agents.sh",
    "clean": "rm -rf packages/*/dist dist"
  },
  "keywords": [
    "product-management",
    "ai",
    "cli",
    "tui",
    "phantom",
    "pm",
    "agent",
    "terminal"
  ],
  "author": "Adarsh Agrahari",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.27.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.74.0",
    "@types/open": "^6.1.0",
    "open": "^11.0.0",
    "openai": "^6.22.0",
    "p-limit": "^7.3.0",
    "playwright": "^1.58.2"
  }
}
